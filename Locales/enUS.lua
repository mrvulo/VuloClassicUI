-- =========================================================
-- VuloClassicUI / Locales / enUS
-- English is the DEFAULT locale — no translations needed because
-- all keys in L["..."] are the English text itself.
-- This file exists so we can register "English support" explicitly
-- and as a reference for translators (the full list of keys).
-- =========================================================
local _, ns = ...

ns:RegisterLocale("enUS", {
    -- Empty: English keys ARE the strings. Fallback handles everything.
})
