-- VuloClassicUI / Locales / enUS
local _, ns = ...

-- Intentionally empty: the L[...] keys ARE the English strings, the fallback
-- returns them unchanged. Registered only to declare English support.
ns:RegisterLocale("enUS", function() return {
} end)
