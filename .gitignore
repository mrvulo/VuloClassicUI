tools/node_modules/
# local-only icon art (third-party licensed — must never ship in a release)
Media/LocalClasses/
# local tooling config, contains absolute machine paths
.claude/
IDEA.md
# editor and agent config: useful locally, but the packager has no ignore rule
# for them, so tracking them would ship them to players inside the addon folder
.vscode/
.mcp.json
# knowledge-graph build artifacts (several MB, regenerable) and the agent
# instruction file that drives them — local tooling, never for players
graphify-out/
CLAUDE.md

.superpowers/
